import React from "react";

// components
import Card from "../Card";

const Draw = ( ) => {
  return (
    <Card colors="green" title="Sorteio de um numero">
      <span>
        <span>Resultado:</span>
        <strong>20</strong>
      </span>
    </Card>
  );
};

export default Draw;
